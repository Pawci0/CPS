<Window x:Class="Visualization.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Visualization"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="800">
    <Window.Resources>
        <ObjectDataProvider x:Key="dataFromEnum" MethodName="GetValues"
                        ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:SignalEnum"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <ComboBox ItemsSource="{Binding Source={StaticResource dataFromEnum}}"
                      SelectedItem="{Binding Path=SelectedSignal}">
            </ComboBox>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                
                <Label Grid.Column="0" Grid.Row="0" Content="Amplitude" Margin="5" />
                <TextBox Grid.Column="0" Grid.Row="1" Name="amplitude" Margin="5"
                         Text="{Binding Amplitude, Mode=TwoWay}"/>

                <Label Grid.Column="1" Grid.Row="0" Content="Begin At" Margin="5" />
                <TextBox Grid.Column="1" Grid.Row="1" Name="beginsAt" Margin="5"
                         Text="{Binding BeginsAt, Mode=TwoWay}"/>

                <Label Grid.Column="2" Grid.Row="0" Content="Duration" Margin="5" />
                <TextBox Grid.Column="2" Grid.Row="1" Name="duration" Margin="5"
                         Text="{Binding Duration, Mode=TwoWay}"/>

                <Label Grid.Column="3" Grid.Row="0" Content="Sampling frequency" Margin="5" />
                <TextBox Grid.Column="3" Grid.Row="1" Name="samplingFrequency" Margin="5"
                         Text="{Binding SamplingFrequency, Mode=TwoWay}"/>

                <Label Grid.Column="4" Grid.Row="0" Content="Period" Margin="5" />
                <TextBox Grid.Column="4" Grid.Row="1" Name="period" Margin="5"
                         Text="{Binding Period, Mode=TwoWay}"/>

                <Button Grid.Column="5" Grid.Row="1" Content="Update" Click="UpdateGraph"/>

            </Grid>
        </StackPanel>
        <lvc:CartesianChart Grid.Row="1" Series="{Binding SeriesCollection}" LegendLocation="Right" />
    </Grid>
       
    
</Window>
